#include "main.h"
/**
 * print_numbers - prints numbers
 * Description: prints from 0 to 9
 * @a: num
 */
void print_numbers(void)
{
	int a;

	for (a = 48; a <= 57; a++)
		_putchar(a);
	_putchar(10);
}
