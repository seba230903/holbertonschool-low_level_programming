#include <stdio.h>
/**
 * main - main
 * Description: print all single digit numbers including zero
 * Return: 0
 */
int main(void)
{
	int p;

	for (p = 48; p <= 57; p++)
		putchar(p);
	putchar(10);
	return (0);
}
